set_charging_mode:
  description: "Set the charging mode of a connector."
  fields:
    serial:
      description: "Serial number of the charging station"
      example: "6220001089"
    mode:
      description: "Charging mode to set (NORMAL, SMART, PAUSED)"
      example: "NORMAL"
    limit:
      description: "Current limit in amperes"
      example: 10
